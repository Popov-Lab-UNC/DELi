general:
  data: "/Users/brandonnovy/Desktop/2_7_25_del6/2_7_DEL_DEL006_cube.csv"
  lib_size: 800000
  output_dir: "test"

#If indexes are not provided, raw_indexes will be used. If only indexes are provided, SD_min and NSC_values can't be calculated 
#due to necessity of raw reads for depth. You can still run deli, but the top of the report will be NA for these values.
#Control cols necessary for MLE, Z-scores

indexes:
  BRD4: ["corrected_index2", "corrected_index3", "corrected_index9"]
  53bp1: ["corrected_index4", "corrected_index6", "corrected_index7"]

control_cols:
  BRD4: ["corrected_index8"]
  53bp1: ["corrected_index8"]

raw_indexes:
  BRD4: ["raw_index2", "raw_index3", "raw_index9"]
  53bp1: ["raw_index4", "raw_index6", "raw_index7"]

flags:
  SD_min: True
  NSC_values: True
  MLE: True
  Z_score: True
  z_score_log_data: True
  normalized_data: False
  disynthon_data: true
  trisynthon_overlap: true
  disynthon_overlap: true
  disynthon_threshold: 20
  simple_spotfire_version: true
  ml_fingerprints_to_RF_reg: true
  ml_fingerprints_to_RF_clf: true
  clf_thresh: 10
  top_hits: 20
  top_hits_metric: "sum"
  report: true
